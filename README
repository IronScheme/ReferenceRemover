ReferenceRemover
================

Removes references from an assembly.

Requires: Mono.Cecil

Usage: ReferenceRemover Assembly [Regex]

If Regex is omitted, the references are listed.

Example: ReferenceRemover IronScheme.Web.Runtime.dll "IronScheme\..*|Oyster.IntX.*"